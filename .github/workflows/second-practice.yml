name: Second GitHub Actions Practice

on:
  push:
    branches:
      - main

jobs:
  second-job:
    runs-on: ubuntu-latest

    steps:
      - name: Second practice message
        run: echo "I am getting better at GitHub Actions"