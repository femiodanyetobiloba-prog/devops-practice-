name: Third GitHub Actions Practice

on:
  push:
    branches:
      - main

jobs:
  third-job:
    runs-on: ubuntu-latest

    steps:
      - name: Learning message
        run: echo "I now understand the basic structure of GitHub Actions"